[project]
name = "mewcode-showcase"
version = "1.0.0"
description = "Browser-safe showcase of a bounded Coding Agent runtime."
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]>=0.128,<1.0",
  "pytest>=8.0,<10.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
