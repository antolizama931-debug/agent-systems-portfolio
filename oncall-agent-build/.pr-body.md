## What changed

- adds a GitHub-style navigation landing page and an incident operations dashboard
- adds a knowledge-base Agent workspace with PDF/Markdown/TXT upload, lexical retrieval, cited DeepSeek answers, and bounded session memory
- replaces demo-only incident fixtures with live GitHub Status ingestion plus a traceable fallback snapshot
- adds Railway deployment configuration and a GitHub Pages deployment workflow

## Why

The previous repository exposed a basic incident-analysis demo. This upgrade turns it into a complete, auditable OnCall Agent portfolio with a separate public navigation page and an interactive Agent project page.

## Safety and deployment

- model credentials remain server-side and `.env` stays ignored
- production write actions remain blocked or approval-only
- GitHub Pages calls the Railway API through the public configuration file
- static asset paths are relative, so the frontend works under `/oncall-agent/`

## Validation

- `19 passed` with `python -m pytest -q`
- staged-file secret scan passed
- `git diff --cached --check` passed
